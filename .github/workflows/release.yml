name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
    
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Create distribution
      run: |
        # Создаем архив с исходным кодом
        tar -czf zeppelin-user-management-${{ github.ref_name }}.tar.gz \
          --exclude='.git*' \
          --exclude='__pycache__' \
          --exclude='*.pyc' \
          --exclude='.venv' \
          --exclude='logs' \
          .
    
    - name: Generate changelog
      id: changelog
      run: |
        # Простой changelog из коммитов
        echo "CHANGELOG<<EOF" >> $GITHUB_OUTPUT
        echo "## Changes in ${{ github.ref_name }}" >> $GITHUB_OUTPUT
        echo "" >> $GITHUB_OUTPUT
        git log --pretty=format:"- %s" $(git describe --tags --abbrev=0 HEAD^)..HEAD >> $GITHUB_OUTPUT 2>/dev/null || echo "- Initial release" >> $GITHUB_OUTPUT
        echo "" >> $GITHUB_OUTPUT
        echo "EOF" >> $GITHUB_OUTPUT
    
    - name: Create Release
      uses: softprops/action-gh-release@v2
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ github.ref_name }}
        name: Release ${{ github.ref_name }}
        body: |
          # Zeppelin User Management ${{ github.ref_name }}
          
          ## 🚀 Features
          - Modern web interface for Apache Zeppelin user management
          - Real-time service monitoring and control
          - Dark/Light theme support
          - Advanced user search and filtering
          - Cross-platform support (Linux/macOS)
          
          ## 📦 Installation
          
          ### Docker (Recommended)
          ```bash
          docker run -d -p 8080:5000 ghcr.io/iwizard7/zeppelin_user_mgmnt:${{ github.ref_name }}
          ```
          
          ### Manual Installation
          1. Download and extract the source code
          2. Install dependencies: `pip install -r requirements.txt`
          3. Run: `python app.py`
          
          ## 📋 Changelog
          ${{ steps.changelog.outputs.CHANGELOG }}
          
          ## 🔗 Links
          - [Documentation](https://github.com/iwizard7/zeppelin_user_mgmnt#readme)
          - [Docker Image](https://ghcr.io/iwizard7/zeppelin_user_mgmnt)
          - [Issues](https://github.com/iwizard7/zeppelin_user_mgmnt/issues)
        files: |
          zeppelin-user-management-${{ github.ref_name }}.tar.gz
        draft: false
        prerelease: false