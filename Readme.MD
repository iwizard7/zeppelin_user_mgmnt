# 🚀 Управление пользователями и мониторинг Zeppelin

## 📋 Обзор
Этот проект предоставляет современный веб-интерфейс для управления пользователями и ролями в Apache Zeppelin с использованием файла `shiro.ini`. Включает полноценный мониторинг статуса сервиса, управление пользователями и детальное логирование всех операций.

## ✨ Возможности
- 🔐 **Управление пользователями**: добавление, удаление и управление пользователями с валидацией данных
- 🛡️ **Управление ролями**: создание, назначение и снятие ролей с пользователей
- 📊 **Мониторинг Zeppelin**: отслеживание статуса сервиса в реальном времени (PID, память, время работы)
- 🎛️ **Управление сервисом**: запуск, остановка и перезапуск Zeppelin одним кликом
- �️ **Мультиплатформенность**: поддержка CentOS, Oracle Linux, Ubuntu, macOS
- � **Детальное логирование**: полный аудит всех действий пользователей с ротацией логов
- 🔄 **Автообновление**: автоматическое обновление статуса каждые 30 секунд
- 🌐 **Современный UI**: Bootstrap интерфейс с flash-уведомлениями и подтверждениями
- 🛡️ **Безопасность**: валидация входных данных, защита от самоудаления, резервное копирование, защита учетной записи admin
- 📄 **Сохранение конфигурации**: изменяются только секции [users] и [roles], остальная конфигурация Zeppelin остается нетронутой

## 📦 Установка и запуск

### 1️⃣ Клонирование репозитория
```bash
git clone https://github.com/iwizard7/zeppelin_user_mgmnt.git
cd zeppelin_user_mgmnt
```

### 2️⃣ Установка зависимостей
Убедитесь, что у вас установлен Python 3.8+

#### Вариант 1: Виртуальное окружение (рекомендуется)
```bash
# Создаем виртуальное окружение
python3 -m venv .venv

# Активируем виртуальное окружение
# На Linux/macOS:
source .venv/bin/activate
# На Windows:
# .venv\Scripts\activate

# Устанавливаем зависимости
pip install -r requirements.txt
```

#### Вариант 2: Системная установка (если разрешено)
```bash
# Для Ubuntu/Debian:
sudo apt install python3-flask

# Или принудительно через pip (не рекомендуется):
pip install -r requirements.txt --break-system-packages
```

#### Вариант 3: Использование pipx
```bash
# Установка pipx (если не установлен)
sudo apt install pipx  # Ubuntu/Debian
# или
brew install pipx      # macOS

# Установка приложения через pipx
pipx install flask
```

### 3️⃣ Настройка прав доступа

#### Для Linux систем с systemctl:
Убедитесь, что пользователь может управлять сервисом Zeppelin:
```bash
# Добавьте в /etc/sudoers.d/zeppelin-mgmt:
your_username ALL=(ALL) NOPASSWD: /bin/systemctl start zeppelin, /bin/systemctl stop zeppelin, /bin/systemctl restart zeppelin, /bin/systemctl status zeppelin, /bin/systemctl is-active zeppelin, /bin/systemctl is-enabled zeppelin
```

#### Для Linux систем с zeppelin-daemon.sh:
Убедитесь, что пользователь может выполнять скрипт:
```bash
# Установите переменную окружения ZEPPELIN_HOME
export ZEPPELIN_HOME=/opt/zeppelin

# Или убедитесь, что скрипт доступен по стандартным путям:
ls -la /opt/zeppelin/bin/zeppelin-daemon.sh
ls -la /usr/local/zeppelin/bin/zeppelin-daemon.sh

# Проверьте права на выполнение
chmod +x $ZEPPELIN_HOME/bin/zeppelin-daemon.sh
```

#### Для macOS:
Права не требуются - приложение работает в демо-режиме для демонстрации функционала.

### 4️⃣ Запуск приложения
```bash
python app.py
```
Приложение будет доступно по адресу: [http://127.0.0.1:5003](http://127.0.0.1:5003)

**Примечание**: Если порт занят, приложение автоматически попробует следующий доступный порт.

### 5️⃣ Первый вход
Используйте любого пользователя из файла `shiro.ini` для входа в систему.

## 🐳 Запуск в Docker

### Сборка образа
```bash
docker build -t zeppelin_user_mgmnt .
```

### Запуск контейнера
```bash
docker run -d -p 5000:5000 --name zeppelin_user_mgmnt zeppelin_user_mgmnt
```
Приложение будет доступно по адресу: [http://localhost:5000](http://localhost:5000)

## 🐳 Docker Compose (рекомендуется)

### Запуск с Docker Compose
```bash
# Создайте директорию для логов
mkdir -p logs

# Запустите контейнер
docker-compose up -d --build
```

Приложение будет доступно по адресу: [http://localhost:8080](http://localhost:8080)

## ⚙️ Настройка `shiro.ini`
Убедитесь, что файл `shiro.ini` существует и имеет правильные права доступа:

```bash
# Установите права доступа
chmod 644 shiro.ini
chown your_user:your_group shiro.ini
```

### Пример структуры `shiro.ini`:
```ini
[users]
admin = admin123, admin
user1 = password1, role1, role2
user2 = password2, role2

[roles]
admin = *
role1 = notebook:*
role2 = notebook:read

[main]
# Конфигурация Shiro
sessionManager = org.apache.shiro.web.session.mgt.DefaultWebSessionManager
securityManager.sessionManager = $sessionManager

[urls]
# URL правила доступа
/api/version = anon
/** = authc
```

### Важные замечания:
- **Пароли**: В текущей версии пароли хранятся в открытом виде (как требует Zeppelin)
- **Роли**: Поддерживаются стандартные Zeppelin роли и разрешения
- **Резервные копии**: Автоматически создаются при каждом изменении
- **Сохранение конфигурации**: Приложение изменяет только секции `[users]` и `[roles]`, все остальные секции (`[main]`, `[urls]` и др.) остаются нетронутыми
- **Порядок секций**: Сохраняется оригинальный порядок секций в файле

## 📊 Мониторинг и управление Apache Zeppelin

### Поддерживаемые операционные системы и способы запуска Zeppelin
- **🐧 CentOS/RHEL**: 
  - systemctl управление сервисами
  - zeppelin-daemon.sh скрипт
- **🔶 Oracle Linux**: 
  - systemctl управление сервисами
  - zeppelin-daemon.sh скрипт
- **🟠 Ubuntu/Debian**: 
  - systemctl управление сервисами
  - zeppelin-daemon.sh скрипт
- **🍎 macOS**: демо-режим (launchctl команды готовы, но работают в демо-режиме)

### Автоматическое определение способа запуска
Приложение автоматически определяет, как запущен Zeppelin:
1. **Systemd сервис** - если найден `zeppelin.service`
2. **Daemon скрипт** - если найден `zeppelin-daemon.sh` в стандартных путях:
   - `/opt/zeppelin/bin/zeppelin-daemon.sh`
   - `/usr/local/zeppelin/bin/zeppelin-daemon.sh`
   - `$ZEPPELIN_HOME/bin/zeppelin-daemon.sh`
   - `./bin/zeppelin-daemon.sh`

### Статус сервиса
Приложение предоставляет детальную информацию о состоянии Zeppelin:
- **Статус**: Запущен/Остановлен/Ошибка/Демо-режим
- **PID процесса**: Идентификатор процесса
- **Использование памяти**: Текущее потребление RAM
- **Время работы**: Как долго сервис работает
- **Автозапуск**: Включен ли автозапуск при загрузке системы
- **Операционная система**: Автоматическое определение ОС

### Управление сервисом
#### Linux (systemctl):
- **▶️ Запуск**: `sudo systemctl start zeppelin`
- **⏹️ Остановка**: `sudo systemctl stop zeppelin`
- **⚡ Перезапуск**: `sudo systemctl restart zeppelin`
- **🔄 Автообновление**: Статус обновляется каждые 30 секунд

#### Linux (zeppelin-daemon.sh):
- **▶️ Запуск**: `$ZEPPELIN_HOME/bin/zeppelin-daemon.sh start`
- **⏹️ Остановка**: `$ZEPPELIN_HOME/bin/zeppelin-daemon.sh stop`
- **⚡ Перезапуск**: `$ZEPPELIN_HOME/bin/zeppelin-daemon.sh restart`
- **📊 Статус**: `$ZEPPELIN_HOME/bin/zeppelin-daemon.sh status`

#### macOS (демо-режим):
- **▶️ Запуск**: Демо-команда (готов launchctl start)
- **⏹️ Остановка**: Демо-команда (готов launchctl stop)
- **⚡ Перезапуск**: Демо-команда (готов launchctl kickstart)

⚠️ **Требования**:
- **Для systemctl**: sudo права для управления systemctl
- **Для daemon**: права на выполнение скрипта zeppelin-daemon.sh

## 📝 Система логирования

### Настройка логирования с ротацией:
- **Директория**: `logs/`
- **Максимальный размер файла**: 10MB
- **Количество backup файлов**: 5
- **Формат**: `YYYY-MM-DD HH:MM:SS - LEVEL - MESSAGE`

### Логируемые действия:
- **Аутентификация**: `LOGIN_SUCCESS/LOGIN_FAILED`, `LOGOUT`
- **Управление пользователями**: `ADD_USER`, `DELETE_USER`, `DELETE_USER_BLOCKED`
- **Управление ролями**: `ADD_ROLE`, `ASSIGN_ROLE`, `UNASSIGN_ROLE`, `ASSIGN_ROLE_BLOCKED`, `UNASSIGN_ROLE_BLOCKED`
- **Управление сервисом**: `START_ZEPPELIN`, `STOP_ZEPPELIN`, `RESTART_ZEPPELIN` (с указанием типа: systemd/daemon)
- **Мониторинг**: `CHECK_ZEPPELIN_STATUS`
- **Безопасность**: Все заблокированные попытки изменения защищенных пользователей

### Пример логов:
```
2025-01-17 14:30:15 - INFO - User: admin, Action: LOGIN_SUCCESS, IP: 192.168.1.100
2025-01-17 14:31:22 - INFO - User: admin, Action: ADD_USER, IP: 192.168.1.100, Details: Added user: newuser
2025-01-17 14:32:10 - INFO - User: admin, Action: CHECK_ZEPPELIN_STATUS, IP: 192.168.1.100
2025-01-17 14:33:45 - INFO - User: admin, Action: RESTART_ZEPPELIN_SUCCESS, IP: 192.168.1.100
```

## 🛡️ Безопасность и валидация

### Валидация входных данных:
- **Длина**: Минимум 3 символа для имен пользователей, 2 для ролей
- **Максимальная длина**: 30 символов для имен, 50 для паролей
- **Запрещенные символы**: `=`, `[`, `]`, `\n`, `\r`
- **Защита от пустых значений**: Автоматическая очистка пробелов

### Дополнительная защита:
- **Резервное копирование**: Автоматическое создание `.backup` файлов
- **Защита от самоудаления**: Нельзя удалить свой аккаунт
- **Защита admin аккаунта**: Учетная запись admin защищена от удаления и изменения ролей
- **Подтверждение операций**: Диалоги подтверждения для критических действий
- **Обработка ошибок**: Graceful handling всех исключений

### Защищенные пользователи:
- **admin**: Не может быть удален, роли не могут быть изменены
- Пароль admin скрыт в интерфейсе (отображается как "*** (защищено)")
- Защищенные пользователи не появляются в выпадающих списках для управления
- Попытки изменения защищенных пользователей логируются как заблокированные действия

### Система защиты admin аккаунта:
1. **Константа защищенных пользователей**: `PROTECTED_USERS = {'admin'}`
2. **Функция проверки**: `is_protected_user()` - автоматическая проверка при всех операциях
3. **Защита от удаления**: Блокировка с логированием `DELETE_USER_BLOCKED`
4. **Защита ролей**: Блокировка назначения/снятия ролей с логированием `ASSIGN_ROLE_BLOCKED`/`UNASSIGN_ROLE_BLOCKED`
5. **Скрытие в UI**: Исключение из всех выпадающих списков управления
6. **Информационные сообщения**: Предупреждения пользователю о защищенных аккаунтах

## 🔧 Технические детали

### Архитектура приложения:
- **Backend**: Flask 2.3.3 (Python)
- **Frontend**: Bootstrap 5.3.0 + JavaScript
- **Логирование**: RotatingFileHandler с автоматической ротацией
- **Конфигурация**: Файл shiro.ini (совместимость с Apache Zeppelin)
- **Мониторинг**: systemctl интеграция для управления сервисами

### Системные требования:
- **Python**: 3.8+
- **ОС**: 
  - Linux с systemd (CentOS, Oracle Linux, Ubuntu, Debian)
  - macOS (демо-режим)
- **Права**: 
  - Linux: sudo доступ к systemctl для управления Zeppelin
  - macOS: демо-режим, реальные права не требуются
- **Память**: Минимум 512MB RAM
- **Диск**: 100MB свободного места для логов

### Структура проекта:
```
zeppelin_user_mgmnt/
├── app.py                 # Основное приложение Flask
├── requirements.txt       # Зависимости Python
├── shiro.ini             # Конфигурация пользователей и ролей
├── Dockerfile            # Docker образ
├── docker-compose.yml    # Docker Compose конфигурация
├── templates/            # HTML шаблоны
│   ├── login.html        # Страница входа
│   └── dashboard.html    # Главная панель
├── static/               # Статические файлы
│   └── style.css         # Дополнительные стили
├── logs/                 # Директория логов (создается автоматически)
└── .gitignore           # Исключения для Git
```

## 🚀 Быстрый старт

### Вариант 1: Docker Compose (рекомендуется)
1. **Клонируйте репозиторий**:
   ```bash
   git clone https://github.com/iwizard7/zeppelin_user_mgmnt.git
   cd zeppelin_user_mgmnt
   ```

2. **Запустите с Docker Compose**:
   ```bash
   mkdir -p logs
   docker-compose up -d --build
   ```

3. **Откройте в браузере**: [http://localhost:8080](http://localhost:8080)

### Вариант 2: Локальный запуск
1. **Клонируйте репозиторий**:
   ```bash
   git clone https://github.com/iwizard7/zeppelin_user_mgmnt.git
   cd zeppelin_user_mgmnt
   ```

2. **Создайте виртуальное окружение и установите зависимости**:
   ```bash
   python3 -m venv .venv
   source .venv/bin/activate  # Linux/macOS
   # .venv\Scripts\activate   # Windows
   pip install -r requirements.txt
   ```

3. **Запустите приложение**:
   ```bash
   python app.py
   ```

4. **Откройте в браузере**: [http://127.0.0.1:5003](http://127.0.0.1:5003)

### Вход в систему
**Войдите в систему** используя данные из `shiro.ini`:
- admin/1234
- user1/password2  
- 222/222
- и другие пользователи из файла

### Особенности по ОС
- **Linux**: Полный функционал с реальным управлением Zeppelin
- **macOS**: Демо-режим для демонстрации интерфейса

## 🔧 Решение проблем

### Ошибка "externally-managed-environment"
Если при установке зависимостей возникает ошибка:
```
error: externally-managed-environment
```

**Решение**: Используйте виртуальное окружение:
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

### Порт уже занят
Если при запуске появляется ошибка "Address already in use":
```bash
# Найдите процесс, использующий порт
lsof -i :5003

# Или измените порт в app.py:
# app.run(host='0.0.0.0', port=5004, debug=False)
```

### Проблемы с правами на Linux
Если команды systemctl не работают:
```bash
# Проверьте права sudo
sudo systemctl status zeppelin

# Добавьте права в sudoers (см. раздел "Настройка прав доступа")
```

### Проблемы с zeppelin-daemon.sh
Если скрипт не найден или не работает:
```bash
# Проверьте наличие скрипта
find /opt /usr/local -name "zeppelin-daemon.sh" 2>/dev/null

# Установите ZEPPELIN_HOME
export ZEPPELIN_HOME=/path/to/your/zeppelin
echo 'export ZEPPELIN_HOME=/path/to/your/zeppelin' >> ~/.bashrc

# Проверьте права на выполнение
chmod +x $ZEPPELIN_HOME/bin/zeppelin-daemon.sh

# Проверьте статус вручную
$ZEPPELIN_HOME/bin/zeppelin-daemon.sh status
```

### Файл shiro.ini не найден
Убедитесь, что файл `shiro.ini` существует в корневой директории проекта:
```bash
ls -la shiro.ini
# Если файла нет, создайте его с базовой конфигурацией
```

### Не могу изменить/удалить пользователя admin
Это нормальное поведение - пользователь admin защищен от изменений:
```bash
# Пользователь admin:
# - Не появляется в выпадающих списках для управления
# - Не может быть удален
# - Роли не могут быть изменены
# - Пароль скрыт в интерфейсе

# Если нужно изменить admin, отредактируйте файл shiro.ini напрямую
```

### Кнопка "Запустить" не работает для daemon
Если кнопка "Запустить" не работает, а "Перезапустить" работает:
```bash
# Проверьте статус daemon
$ZEPPELIN_HOME/bin/zeppelin-daemon.sh status

# Попробуйте запустить вручную
$ZEPPELIN_HOME/bin/zeppelin-daemon.sh start

# Проверьте логи приложения для диагностики
tail -f logs/app.log
```

## 🆕 Последние обновления

### v2.0 - Мультиплатформенность и защита admin
- ✅ **Поддержка zeppelin-daemon.sh**: Автоматическое определение способа запуска Zeppelin
- ✅ **Защита admin аккаунта**: Полная защита от удаления и изменения ролей
- ✅ **Улучшенное логирование**: Детальное логирование всех операций с указанием типа сервиса
- ✅ **Автоопределение ОС**: Поддержка CentOS, Oracle Linux, Ubuntu, macOS
- ✅ **Демо-режим для macOS**: Полнофункциональный интерфейс без реального управления сервисами
- ✅ **Улучшенная обработка ошибок**: Более информативные сообщения об ошибках
- ✅ **Сохранение конфигурации**: Изменяются только секции [users] и [roles]

### v1.0 - Базовый функционал
- ✅ **Управление пользователями и ролями**: Добавление, удаление, назначение ролей
- ✅ **Мониторинг Zeppelin**: Отслеживание статуса сервиса в реальном времени
- ✅ **Система логирования**: Ротация логов и аудит действий пользователей
- ✅ **Веб-интерфейс**: Bootstrap UI с flash-уведомлениями

## 📜 Лицензия
Этот проект распространяется под лицензией MIT.

## 💡 Авторы
- [iwizard7](https://github.com/iwizard7)

### ⭐️ Буду рад звезде на GitHub, если проект оказался полезен! 😊